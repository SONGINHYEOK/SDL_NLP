# OmniLNP 2분 데모 영상 — 자막 + 실행 가이드

> **사전 준비:** `python manage.py runserver` → `http://127.0.0.1:8000/` 접속 → 로그인 완료

---

### 0:00 — 대시보드
**자막:** 43개 논문, 19,797건 실험, 13,339종 이온화 지질 — 통합 LNP 데이터베이스
```
현재 위치: / (로그인 후 자동 이동)
할 일: 상단 통계 카드 5개를 왼쪽→오른쪽으로 천천히 마우스 이동
```

### 0:07 — 대시보드 (스크롤)
**자막:** AI가 설계하고, 장비가 합성하고, 결과를 학습하는 자율 최적화 파이프라인
```
할 일: 아래로 스크롤 → "자율 최적화 파이프라인" 5단계 다이어그램이 보이면 멈추기
```

### 0:13 — 지질 탐색기
**자막:** 13,339종 이온화 지질을 검색하고 필터링합니다
```
이동: 사이드바 왼쪽 > "데이터" 섹션 > "지질 탐색기" 클릭
할 일: 검색창에 "MC3" 입력 → "필터" 버튼 클릭
```

### 0:19 — 지질 상세
**자막:** 각 지질의 2D 구조, 물성, 레이더 차트를 한눈에 분석합니다
```
이동: 테이블에서 아무 지질 이름(파란 링크) 클릭
할 일: 2D 구조 → 물성 카드 → 오른쪽 레이더 차트 순서로 마우스 이동
```

### 0:26 — 지질 비교
**자막:** 최대 4개 지질을 나란히 놓고 물성을 비교합니다
```
이동: 브라우저 주소창에 http://127.0.0.1:8000/compounds/compare/ 직접 입력
할 일: 검색창에 지질 이름 입력 → 2~3개 추가 → 레이더 오버레이 차트 보여주기
```

### 0:33 — 제형 설계 도구
**자막:** 4성분 슬라이더로 LNP 제형을 설계하고 AI가 실시간 효능을 예측합니다
```
이동: 사이드바 > "데이터" > "제형 목록" 클릭 → 페이지 상단 "+ 새 설계" 버튼 클릭
      (또는 주소창에 http://127.0.0.1:8000/formulations/designer/ 직접 입력)
할 일: ① "연구 빈도 상위"에서 아무 지질 클릭
      ② 4개 슬라이더 드래그 (50 / 10 / 38.5 / 1.5)
```

### 0:42 — 제형 설계 도구 (AI 예측)
**자막:** 도넛 차트가 조성 비율에 따라 실시간으로 변합니다
```
할 일: 슬라이더를 천천히 움직이면서 도넛 차트 변화 보여주기
      → 오른쪽 상단 "AI 예측 실행" 버튼 클릭
```

### 0:48 — AI 구조 생성
**자막:** AI가 목표 물성에 맞는 신규 이온화 지질 구조를 자동 생성합니다
```
이동: 사이드바 > "AI 모델" 섹션 > "구조 생성" 클릭
할 일: MW Min 500 / MW Max 900 / LogP Min 8 / LogP Max 15 입력
      → "후보 생성" 버튼 클릭
```

### 0:55 — AI 물성 예측
**자막:** 생성된 후보의 SMILES를 넣으면 분자 물성을 바로 예측합니다
```
이동: 사이드바 > "AI 모델" > "물성 예측" 클릭
할 일: SMILES 텍스트 영역에 아무 SMILES 붙여넣기 (또는 "등록된 지질 선택"에서 검색)
      → "예측 실행" 버튼 클릭
```

### 1:02 — 다목적 최적화
**자막:** 삼성분 조성 공간과 파레토 전선으로 다목적 최적화를 수행합니다
```
이동: 사이드바 > "AI 모델" > "최적화" 클릭
할 일: 삼각 다이어그램 위 점에 마우스 올려 툴팁 보여주기
      → 아래로 스크롤해서 파레토 전선 차트 보여주기
```

### 1:10 — 다목적 최적화 (실행)
**자막:** 최적화를 실행하면 반복마다 최적 조성이 수렴합니다
```
할 일: 오른쪽 패널에서 목적 함수 "균형 (다목적)" 선택
      → "최적화 실행" 버튼 클릭 → 진행 차트 + 최적 제형 결과 보여주기
```

### 1:17 — 장비 모니터링
**자막:** 연결된 장비의 상태와 프로토콜을 실시간으로 모니터링합니다
```
이동: 사이드바 > "자율실험실" 섹션 > "장비 모니터링" 클릭
할 일: 장비 카드 그리드를 천천히 훑기 (상태 표시등 색상 + 프로토콜 뱃지)
```

### 1:23 — 장비 유지보수
**자막:** 캘리브레이션 기한 초과 알림과 정비 일정을 관리합니다
```
이동: 사이드바 > "자율실험실" > "장비 유지보수" 클릭
할 일: 상단 빨간 "기한 초과 정비" 패널 → 아래로 "예정 정비" → "완료 이력" 순서로 훑기
```

### 1:29 — 시약 재고
**자막:** 시약 재고 수준과 부족 알림을 한눈에 파악합니다
```
이동: 사이드바 > "자율실험실" > "시약 재고" 클릭
할 일: 왼쪽 "부족 알림" 빨간 바 보여주기 → 오른쪽 "전체 시약" 테이블 훑기
```

### 1:35 — 워크플로우
**자막:** 5단계 파이프라인을 자동으로 실행하고 각 단계 결과를 확인합니다
```
이동: 사이드바 > "자율실험실" > "워크플로우" 클릭
할 일: 기존 워크플로우 런 카드의 5단계 진행 표시 보여주기 → "상세" 클릭
```

### 1:43 — 워크플로우 상세
**자막:** 각 단계를 승인하면 장비가 자동 실행하고 시약이 자동 차감됩니다
```
할 일: "승인 및 계속" 버튼 클릭 → "시뮬레이션" 버튼 클릭
      → "출력 데이터" 펼쳐서 결과 JSON 보여주기
```

### 1:50 — AI 보고서
**자막:** AI가 전체 실험 결과를 분석하여 전문 보고서를 자동 생성합니다
```
할 일: 페이지 상단 "보고서 생성" 버튼 클릭 → 생성된 보고서 내용 스크롤
```

### 1:55 — 마무리
**자막:** OmniLNP — 설계부터 학습까지, AI 기반 자율실험실 플랫폼
```
이동: 사이드바 > "개요" > "대시보드" 클릭
할 일: 파이프라인 다이어그램이 보이는 상태에서 마무리
```
